module.exports = {
    publicPath: process.env.NODE_ENV === 'production'
      ? '/'
      : '/'
      ? '/tutorials'
      : '/tutorials'
      ? '/add'
      : '/add'
      ? '/12'
      : '/add'
  }